## Now you can run:

```shell


sudo ./install_tb.sh dev
sudo ./install_tb.sh prod

```

## Each will create and manage its own service:

```shell


sudo systemctl status tigerbeetle-dev
sudo systemctl status tigerbeetle-prod
```